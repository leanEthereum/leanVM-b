# Current 127-bit proof status

The public theorem remains 126 bits. The original-game 127-bit bound is established for every $q\ge3\cdot2^{114}$; the smaller positive-budget interval remains open. This is the status entry point; the other notes supply mathematical derivations rather than a chronological progress log.

Put $N=2^{128}$, $x=q/N$, $\delta=11/65536$ and $\epsilon(q)=q/2^{169}+2^{-700}$. The [closing contract](minimal-closing-contract.md) targets $2x-x^2+\delta x+\epsilon(q)$ for $3\cdot2^{114}\le q<2^{127}$ and $(15/8+\delta)x+\epsilon(q)$ below that split. Both suffice for the 127-bit slope. Budgets at least $2^{127}$ use probability at most one.

## Established interfaces

| Module | Result |
| --- | --- |
| [RetainedResidualOriginalBudget](../SphincsSecurity/Proof/RetainedResidualOriginalBudget.lean) | The unchanged original hash bound supplies the retained source budget, including key generation, failures, repeated calls and final verification. |
| [RetainedResidualTerminalCoverage](../SphincsSecurity/Proof/RetainedResidualTerminalCoverage.lean) | $\mathbb E C_{\rm full}\le\mathbb E D_{\rm native}/N+\delta x$, with proposal erasure and the actual native message count. |
| [RetainedResidualPrimitivePotential](../SphincsSecurity/Proof/RetainedResidualPrimitivePotential.lean) | $\Pr[B]+\mathbb E D_{\rm native}/N\le2x-x^2$, hence $\Pr[B]+\mathbb E C_{\rm full}\le2x-x^2+\delta x$. Stopped runs retain their earlier message costs. |
| [RetainedResidualGameTransfer](../SphincsSecurity/Proof/RetainedResidualGameTransfer.lean) | Original SUF success is bounded by a primitive stop or a live strong forgery, retaining the full signing log. |
| [RetainedResidualVerifySupport](../SphincsSecurity/Proof/RetainedResidualVerifySupport.lean) | Supported accepting verification has honest openings at all layers and disclosure of every required FTS secret. |
| [RetainedResidualReplay](../SphincsSecurity/Proof/RetainedResidualReplay.lean) | The recovered signature equals the original `signAfterDigest` result, including counters and every signature field. |
| [RetainedResidualSigningHistory](../SphincsSecurity/Proof/RetainedResidualSigningHistory.lean) | Every successful log entry has its original signing computation and cached digest; every disclosed FTS leaf has a log witness. The invariant holds initially and is preserved through the source execution. |
| [RetainedResidualStrongCoverage](../SphincsSecurity/Proof/RetainedResidualStrongCoverage.lean) | A supported, accepting strong forgery yields `TargetCertificateAt` for all FTS trees in the retained cache and log. Canonical replay excludes the forgery's own input from every signing witness. |
| [RetainedResidualBankCompleteness](../SphincsSecurity/Proof/RetainedResidualBankCompleteness.lean) | An active native monitor has the same signing log as the source memory and counts every full certificate in that memory's cache and log. |
| [RetainedResidualCertificateTransfer](../SphincsSecurity/Proof/RetainedResidualCertificateTransfer.lean) | On the actual initial monitored law, a supported strong forgery with an active monitor has bank count at least one. Its probability is bounded by the expected bank count plus successful strong forgeries with the monitor stopped. |
| [RetainedResidualMonitoredGame](../SphincsSecurity/Proof/RetainedResidualMonitoredGame.lean) | Averaging the initial sampling and erasing the monitor gives $\Pr[\mathrm{SUF}]\le2x-x^2+\delta x+\Pr[E_{\rm native}]$ from the unchanged original query bound, for $q\le2^{127}$. This intermediate theorem leaves the exception explicit; the history bounds below discharge it. |
| [RetainedResidualAccounting](../SphincsSecurity/Proof/RetainedResidualAccounting.lean) | An active monitor's spent count equals the native hash-call count. Supported steps account for minimum macro costs and signing-log growth; valid bounded continuations supply the earlier signing-cap and macro-budget conditions. |
| [RetainedResidualCacheAccounting](../SphincsSecurity/Proof/RetainedResidualCacheAccounting.lean) | The native cache size never exceeds its actual hash-call count, including on stopped runs. Together with exact spending, this supplies the proposal cache bound outside the cache exceptions. |
| [RetainedResidualMonitorReadiness](../SphincsSecurity/Proof/RetainedResidualMonitorReadiness.lean) | Successful signing digests remain cached through every native step, including signing failures. Before a supported query on a valid bounded continuation, an unstopped monitor with no cache exception is active. |
| [RetainedResidualMonitorStops](../SphincsSecurity/Proof/RetainedResidualMonitorStops.lean) | A supported live step has an exact operational record. An active step with no resulting cache exception satisfies the post-step readiness conditions; with the default proposal rule, its stopped flag is equivalent to the proposal-prefix exception. |
| [RetainedResidualExceptionGame](../SphincsSecurity/Proof/RetainedResidualExceptionGame.lean) | Passive cache and proposal-prefix history flags erase to the monitored source law. Every successful strong forgery with a stopped monitor lies in one of these history events, yielding an original SUF bound with their two explicit probabilities. |
| [ProposalPrefixExponential](../SphincsSecurity/Proof/ProposalPrefixExponential.lean) | The geometric block-length moment, its exponential weight, threshold domination and initial weight at most $2^{-700}$ are proved with exact constants. |
| [RetainedResidualProposalTail](../SphincsSecurity/Proof/RetainedResidualProposalTail.lean) | The exponential weight bounds the proposal-prefix history probability on the actual adaptive native law by $2^{-700}$. Consequently original SUF probability is at most $2x-x^2+\delta x+\Pr[E_{\rm cache}]+2^{-700}$ from the original query bound. |
| [CertificateCacheExceptionGrowth](../SphincsSecurity/Proof/CertificateCacheExceptionGrowth.lean) | A second-moment cache weight dominates the exceptional predicate, starts at zero and grows by at most $1023/2^{186}+2^{-170}<2^{-169}$ per fresh random-oracle hash. It bounds the history event for an ordinary bounded random-oracle computation. |
| [RetainedResidualCacheKernels](../SphincsSecurity/Proof/RetainedResidualCacheKernels.lean) | Native world queries and complete signing calls obey the cache-weight estimate, charging only message calls. Non-message queries preserve message answers and extend the cache, so they cannot increase the weight. Their charge applies even after the certificate monitor stops. |
| [RetainedResidualCacheHistory](../SphincsSecurity/Proof/RetainedResidualCacheHistory.lean) | Freezing the weight at the first exception bounds the entire passive cache history by accumulated native message charges, including stopped runs. |
| [RetainedResidualPaymentBudget](../SphincsSecurity/Proof/RetainedResidualPaymentBudget.lean) | Native message charges are bounded by actual hash-call growth and accumulate to at most the original hash budget. |
| [RetainedResidualCacheTail](../SphincsSecurity/Proof/RetainedResidualCacheTail.lean) | The actual native cache-history probability is at most $q/2^{169}$, giving the original-game bound $2x-x^2+\delta x+\epsilon(q)$. |
| [Security127LargeBudget](../SphincsSecurity/Proof/Security127LargeBudget.lean) | Exact closing arithmetic and probability at most one prove the original SUF bound $q/2^{127}$ for every $q\ge3\cdot2^{114}$. The reference dummy is concretely instantiated. |
| [AdaptiveChainEndpoint](../SphincsSecurity/Proof/AdaptiveChainEndpoint.lean) | Adaptive prefix likelihood and allocated cost comparison for the generic causal interface. |
| [OtsPrefixSimulation](../SphincsSecurity/Proof/OtsPrefixSimulation.lean) | The prefix-query interpreter reconstructs the original fixed-oracle game with its signing log, verifier and hash-cost trace. Arbitrary replacement of private prefix entries in the auxiliary oracle leaves the program unchanged. |
| [ReferenceFamilySeed](../SphincsSecurity/Proof/ReferenceFamilySeed.lean) | A uniform encoding seed over a fixed domain replaces the secret-dependent domain of unused encoding rows without changing the reference-family oracle law. |
| [OtsPrefixRawSampling](../SphincsSecurity/Proof/OtsPrefixRawSampling.lean) | Prefix inputs lie in the finite graph input set and outside the encoding domains. The raw nonencoding oracle splits into independent low prefix tables, high prefix replies and remaining rows. |
| [OtsPrefixReferenceSeed](../SphincsSecurity/Proof/OtsPrefixReferenceSeed.lean) | Exact original oracle sampling from the split seeds, allowing the reference family to choose the prefix depth. The rebuilt oracle's prefix replies equal those sampled rows. |
| [OtsPrefixFrontier](../SphincsSecurity/Proof/OtsPrefixFrontier.lean) | The selected endpoint determines its frontier value; other chains are independent of its private prefix. The canonical encoding messages are reconstructed from these frontiers. |
| [OtsPrefixRawOracle](../SphincsSecurity/Proof/OtsPrefixRawOracle.lean) | The raw oracle separates into explicit prefix queries and an auxiliary oracle with fixed private-prefix entries, preserving all other byte inputs. |
| [OtsPrefixSeedReconstruction](../SphincsSecurity/Proof/OtsPrefixSeedReconstruction.lean) | The sampled oracle and frontier factor through the endpoint. Their auxiliary reconstruction is unchanged by replacing the selected starting-secret field. |
| [OtsPrefixSeedGame](../SphincsSecurity/Proof/OtsPrefixSeedGame.lean) | Exact fixed-seed game correspondence with the endpoint-supplied prefix program and its chosen secret field set to zero. |
| [OtsPrefixSourceGame](../SphincsSecurity/Proof/OtsPrefixSourceGame.lean) | The endpoint source has exactly the original forgery probability and obeys the original whole-experiment hash budget. |
| [OtsPrefixSecretSampling](../SphincsSecurity/Proof/OtsPrefixSecretSampling.lean) | The uniform secret table splits into an independent chosen digest and a table with that coordinate erased. |
| [OtsPrefixObservedSource](../SphincsSecurity/Proof/OtsPrefixObservedSource.lean) | The original source is exactly a mixture of generic observed-prefix real runs, after discarding endpoint and prefix-row observations. |
| [AdaptiveChainSupport](../SphincsSecurity/Proof/AdaptiveChainSupport.lean) | With a syntactic prefix bound $q<N$, the positive likelihood factor gives a pointwise probability comparison and inclusion of ideal support in real support. |
| [OtsPrefixObservedBudget](../SphincsSecurity/Proof/OtsPrefixObservedBudget.lean) | Each supported conditional real run inherits the original whole-experiment hash bound. |
| [QueryCap](../SphincsSecurity/Proof/QueryCap.lean) and [QueryCapErasure](../SphincsSecurity/Proof/QueryCapErasure.lean) | The cap has an unconditional syntactic query bound. Its distribution equals the complete counted run with over-budget outcomes mapped to an ordinary cap result; within budget, erasure preserves the original result distribution. |
| [OtsPrefixAccounting](../SphincsSecurity/Proof/OtsPrefixAccounting.lean) | Every supported execution of the endpoint-supplied OTS program has prefix-query count at most its original hash-cost trace. This holds for arbitrary endpoints and includes the verifier. |
| [AdaptiveChainCap](../SphincsSecurity/Proof/AdaptiveChainCap.lean) | The real hash bound and prefix accounting imply exact real cap erasure, including recovery of the uncapped query count from the remaining budget. For $q<N$, ideal capped runs never stop and inherit the original output-cost bound. |
| [OtsPrefixCappedSource](../SphincsSecurity/Proof/OtsPrefixCappedSource.lean) | The concrete capped OTS interface follows from the original query bound for every supported conditional source, with no additional prefix-query-bound premise. |
| [CausalFrontierProgram](../SphincsSecurity/Proof/CausalFrontierProgram.lean) and [CausalFrontierAllocation](../SphincsSecurity/Proof/CausalFrontierAllocation.lean) | One external-query program reconstructs the fixed-oracle game and each prefix interpreter. Its recorded external hash calls are bounded by the original hash-cost trace. |
| [OtsPrefixAllocation](../SphincsSecurity/Proof/OtsPrefixAllocation.lean) | Different chain addresses have disjoint prefix queries. Each fixed-oracle prefix counter equals the corresponding counter in the common external-query program. |
| [ReferenceFamilyAllocation](../SphincsSecurity/Proof/ReferenceFamilyAllocation.lean) | Recording the common program's queries preserves original SUF probability. The sum of all OTS-prefix query counts is at most the original budget, both on each supported run and in expectation. |
| [OtsPrefixSeedAllocation](../SphincsSecurity/Proof/OtsPrefixSeedAllocation.lean) and [OtsPrefixObservedAllocation](../SphincsSecurity/Proof/OtsPrefixObservedAllocation.lean) | The original result and selected-prefix counter are preserved jointly through reference sampling and the independent-secret split. After averaging the conditional samples, the sum of the real expected counts is at most the original budget. |
| [AdaptiveChainCapCost](../SphincsSecurity/Proof/AdaptiveChainCapCost.lean) | The capped likelihood bound transfers ideal spent-query cost to the uncapped real counter. Supported sampling preserves this scaled expectation inequality. |
| [OtsPrefixIdealAllocation](../SphincsSecurity/Proof/OtsPrefixIdealAllocation.lean) | Averaging over the actual conditional samples gives $(1-q/N)\sum_c\mathbb E Q_c^{\rm ideal}\le q$ using only the original query bound. Below $N$, the cap never terminates, so these are the completed ideal runs' query counts. |
| [BoundaryChargePartition](../SphincsSecurity/Proof/BoundaryChargePartition.lean) | The original trace partitions into proper message calls and non-message calls. External non-message work is bounded by its own trace share, reserving all internal message work. |
| [QueryClassAllocation](../SphincsSecurity/Proof/QueryClassAllocation.lean) | External non-message queries partition into disjoint OTS-prefix, encoding and other classes at the actual public parameter. |
| [ReferenceQueryAllocation](../SphincsSecurity/Proof/ReferenceQueryAllocation.lean) | $Q+A_{\rm enc}+A_{\rm other}+A_{\rm msg}\le q$ on supported original executions, and $(1-q/N)\sum_c\mathbb E Q_c^{\rm ideal}+\mathbb E_R[A_{\rm enc}+A_{\rm other}+A_{\rm msg}]\le q$. The external classes count repetitions conservatively; the native message trace includes signing internals and verification. |
| [AdaptiveChainContact](../SphincsSecurity/Proof/AdaptiveChainContact.lean) | For an arbitrary adaptive prefix program starting with no observed rows, the real probability of querying a last-level row that returns the published endpoint is at most $2\mathbb E_I Q/N$. The counter records the actual number of selected-prefix queries. Preparation at earlier levels is included through a pending-path potential. |
| [AdaptiveChainCapContact](../SphincsSecurity/Proof/AdaptiveChainCapContact.lean) | A completed capped run's explicit counter equals its spent-budget field. The first-contact estimate uses exactly the capped ideal cost in the joint allocation. |
| [AdaptiveChainCapObservation](../SphincsSecurity/Proof/AdaptiveChainCapObservation.lean) | Cap erasure preserves the endpoint, original result and observed prefix rows jointly, so the uncapped real contact event has the same probability. |
| [OtsPrefixContactProbability](../SphincsSecurity/Proof/OtsPrefixContactProbability.lean) | Averaging over the concrete conditional source gives $P_c\le2\mathbb E_I Q_c/N$ and $(1-x)\sum_c P_c+(2/N)\mathbb E_R[A_{\rm enc}+A_{\rm other}+A_{\rm msg}]\le2q/N$, using only the original hash bound and $q<N$. |
| [AdaptiveChainRestart](../SphincsSecurity/Proof/AdaptiveChainRestart.lean) | For a fixed endpoint and partial table with no contact, the weighted later-contact mass is charged to old distinct prefix rows plus twice future queries. The budget-dependent likelihood lower bound transfers that charge to the real weighted completion law. |
| [AdaptiveChainCheckpoint](../SphincsSecurity/Proof/AdaptiveChainCheckpoint.lean) and [AdaptiveChainCheckpointContact](../SphincsSecurity/Proof/AdaptiveChainCheckpointContact.lean) | The real law retains the endpoint, checkpoint and continuation jointly. The posterior identity transports the restart charge to this law: $(1-x)N\Pr[\mathrm{marked},\mathrm{laterContact}]\le\mathbb E_R[(a+2Q^+)\mathbf1_{\rm marked}]$ when the selected chain is uncontacted at the checkpoint. |
| [QueryPause](../SphincsSecurity/Proof/QueryPause.lean) and [AdaptiveChainPause](../SphincsSecurity/Proof/AdaptiveChainPause.lean) | An adaptive query program can pause on retained memory and resume with the same final result and observed rows. Its past counter plus remaining query bound fits the original syntactic budget, deriving the checkpoint's cost hypotheses and the selected-chain marked restart bound. |
| [AdaptiveChainAuxiliary](../SphincsSecurity/Proof/AdaptiveChainAuxiliary.lean) and [OtsPrefixVisible](../SphincsSecurity/Proof/OtsPrefixVisible.lean) | Hash calls outside the selected prefix remain explicit auxiliary queries. Erasing them recovers the existing concrete seed program; the real law, selected counter and capped real/ideal laws agree. Replacing the selected starting-secret field still leaves the program unchanged. |
| [QueryPauseTranslation](../SphincsSecurity/Proof/QueryPauseTranslation.lean) and [OtsPrefixVisibleTrace](../SphincsSecurity/Proof/OtsPrefixVisibleTrace.lean) | The visible interpreter preserves the external hash input/output trace of the common causal program. Every stop defined from that trace commutes with translation to the selected-prefix interpreter, retaining the same checkpoint trace and translated continuation. |
| [OtsContactTrace](../SphincsSecurity/Proof/OtsContactTrace.lean) and [OtsContactPause](../SphincsSecurity/Proof/OtsContactPause.lean) | Contact is defined by an observed last-prefix hash reply equal to its frontier endpoint. One hash query contacts at most one chain, so the common first-contact checkpoint has at most one contacted chain. |
| [QueryPauseTrace](../SphincsSecurity/Proof/QueryPauseTrace.lean) and [OtsContactSplit](../SphincsSecurity/Proof/OtsContactSplit.lean) | Pausing and resuming preserves the full external trace. Two contacted chains imply a marked checkpoint and a later contact with a chain uncontacted there. The common program's external trace length is bounded by its original hash-cost trace. |
| [OtsContactAllocation](../SphincsSecurity/Proof/OtsContactAllocation.lean) | The sum of past selected calls plus twice future selected calls over any subset of chains is at most the corresponding common trace cost, hence at most twice the original budget. |
| [OtsPrefixVisibleContact](../SphincsSecurity/Proof/OtsPrefixVisibleContact.lean) and [OtsContactCheckpoint](../SphincsSecurity/Proof/OtsContactCheckpoint.lean) | On supported lazy checkpoints, contact in the external trace is equivalent to contact in the selected partial table, and its distinct queried rows are bounded by its trace count. This also holds with the explicit prefix counter retained. |
| [ReferenceInstrumentedGame](../SphincsSecurity/Proof/ReferenceInstrumentedGame.lean), [OtsPrefixInstrumentedSeed](../SphincsSecurity/Proof/OtsPrefixInstrumentedSeed.lean) and [OtsPrefixInstrumentedSource](../SphincsSecurity/Proof/OtsPrefixInstrumentedSource.lean) | An observer of the common program preserves its full result through the reference seed and secret split. An erasing observer preserves the original SUF result and hash-cost trace. |
| [ReferenceContactGame](../SphincsSecurity/Proof/ReferenceContactGame.lean) and [OtsContactSourceAllocation](../SphincsSecurity/Proof/OtsContactSourceAllocation.lean) | The first-contact observer retains the frontier, both external traces and original result. All conditional chain sources project to the same law, and the expected sum of marked restart charges is at most $2q$ times its first-contact probability. |
| [OtsPrefixInstrumentedVisible](../SphincsSecurity/Proof/OtsPrefixInstrumentedVisible.lean), [AdaptiveChainCheckpointProjection](../SphincsSecurity/Proof/AdaptiveChainCheckpointProjection.lean) and [OtsContactCheckpointLaw](../SphincsSecurity/Proof/OtsContactCheckpointLaw.lean) | The joint checkpoint law projects to the conditional observed trace law, retaining both traces and final partial-table rows. The projection erases the auxiliary prefix counters and private checkpoint rows. |
| [QueryCapState](../SphincsSecurity/Proof/QueryCapState.lean) and [AdaptiveChainActualBudget](../SphincsSecurity/Proof/AdaptiveChainActualBudget.lean) | A stateful cap, with its state erased, has the same result as finishing the complete counted run. The established real cost bound and $q<N$ therefore bound every uncapped lazy count and its paused continuation. `pausedRun_newContact_charge_of_real` derives the allocated restart inequality from these actual cost hypotheses. |
| [OtsContactProbability](../SphincsSecurity/Proof/OtsContactProbability.lean) and [OtsContactFirstProbability](../SphincsSecurity/Proof/OtsContactFirstProbability.lean) | The concrete checkpoint's final contact and query charges are transported to the common original law. The original trace-contact event has exactly the previously bounded prefix-contact distribution. |
| [OtsDistinctContactBound](../SphincsSecurity/Proof/OtsDistinctContactBound.lean) | For $q<N$, $\Pr_R[E_2]\le4x\mathbb E_R Q_{\rm prefix}/[(1-x)^2N]$, with a joint bound reserving encoding, other hashes and message calls. |
| [EncodingBackwardWitness](../SphincsSecurity/Proof/EncodingBackwardWitness.lean) and [EncodingNeighbors](../SphincsSecurity/Proof/EncodingNeighbors.lean) | Equal-sum words give equality, a unit neighbor, one decrease of at least two, or decreases on two distinct chains. A unit neighbor has a unique lowered coordinate; there are at most 41 words for one lowered chain and 1722 overall. |
| [EncodingFreshRow](../SphincsSecurity/Proof/EncodingFreshRow.lean) and [OtsEncodingMarker](../SphincsSecurity/Proof/OtsEncodingMarker.lean) | The concrete decoder gives the neighbor bounds for unrestricted, invalid and reference-word row supports. Markers are defined on the common hash trace; one query marks at most one chain and cannot also be a prefix contact. Their adaptive original-law accumulation and both chronological estimates are proved below; the accepting-forgery classification remains required. |
| [AdaptiveChainContactMoments](../SphincsSecurity/Proof/AdaptiveChainContactMoments.lean) | In the ideal adaptive prefix law, the last-edge hit count satisfies $\mathbb E K\le\mathbb E Q/N$ and $\mathbb E[K(K-1)]\le2q\mathbb E Q/N^2$ under a syntactic prefix bound. These supply the $(4x+2x^2)\mathbb E Q/N$ correction. |
| [PartialChainTwoEdge](../SphincsSecurity/Proof/PartialChainTwoEdge.lean) | A first two-edge completion occurs only at one of the final two levels. Both chronological orders have exact ideal one-step probabilities, and before completion the endpoint density is at most $1+K$. |
| [PartialChainRowCharge](../SphincsSecurity/Proof/PartialChainRowCharge.lean) | Updating a row raises the endpoint density by at most the incoming prefix density. The two completion orders have weighted risks $(2+K+M)/N$ on productive last queries and $K(2+K+M)/N$ on penultimate queries, with the appropriate earlier-path count $M$. |
| [PartialChainLongCompletion](../SphincsSecurity/Proof/PartialChainLongCompletion.lean) and [PartialChainPreparation](../SphincsSecurity/Proof/PartialChainPreparation.lean) | Completed suffix paths and productive last inputs grow monotonically. Fresh last and penultimate observations pay their earlier-path counts, and the baseline preparation charge is at most $3Q/2$. |
| [PartialChainTwoEdgeCharge](../SphincsSecurity/Proof/PartialChainTwoEdgeCharge.lean) and [PartialChainTwoEdgeCompensation](../SphincsSecurity/Proof/PartialChainTwoEdgeCompensation.lean) | The accumulated charge covers both weighted first-completion risks. After completion, the density martingale preserves the event weight. |
| [AdaptiveChainCompensation](../SphincsSecurity/Proof/AdaptiveChainCompensation.lean) and [AdaptiveChainTwoEdge](../SphincsSecurity/Proof/AdaptiveChainTwoEdge.lean) | A two-potential telescope with finite one-step charge expectation yields $\Pr_R[\mathrm{twoEdge}]\le(3/2+4x+2x^2)\mathbb E_I Q/N$. The actual selected-query counter bounds observed rows; the prefix budget bounds the terminal correction. |
| [AdaptiveChainCapTwoEdge](../SphincsSecurity/Proof/AdaptiveChainCapTwoEdge.lean) and [OtsPrefixTwoEdgeProbability](../SphincsSecurity/Proof/OtsPrefixTwoEdgeProbability.lean) | The original real cost bound gives the same two-edge estimate against capped ideal spent cost, including variable prefix depths. Averaging the concrete source and summing over chains retains the original joint reservation for encoding, other hashes and native message calls. |
| [ProposalQueryProjection](../SphincsSecurity/Proof/ProposalQueryProjection.lean) | Adaptive erasure of rejected proposal values to signing records and independent block lengths. |
| [ReferenceEncodingErasure](../SphincsSecurity/Proof/ReferenceEncodingErasure.lean) and [ReferenceEncodingSource](../SphincsSecurity/Proof/ReferenceEncodingSource.lean) | For fixed key samples, reference selections and non-encoding rows determine the causal adversary program. Replacing its private encoding table by a representative preserves the original instrumented law, including signing failures, counters, root, traces and costs. |
| [UniformTableObservation](../SphincsSecurity/Proof/UniformTableObservation.lean) and [EncodingConditionalObservation](../SphincsSecurity/Proof/EncodingConditionalObservation.lean) | Exact adaptive posterior for row disclosure, retaining the latent table and execution result, specialized to first-success encoding tables including exhaustion. Initial allowed rows satisfy the fresh-row kernel condition. |
| [EncodingTablePrior](../SphincsSecurity/Proof/EncodingTablePrior.lean), [EncodingOracleObservation](../SphincsSecurity/Proof/EncodingOracleObservation.lean) and [ReferenceEncodingLazySource](../SphincsSecurity/Proof/ReferenceEncodingLazySource.lean) | The conditional reference family and remaining encoding rows form one restricted uniform table. The original byte program has exactly its lazy observation distribution for any observer, preserving results, traces and costs. |
| [EncodingMarkerKernel](../SphincsSecurity/Proof/EncodingMarkerKernel.lean) | The combined prior satisfies the $41/N$ and $1722/N$ fresh-row marker kernels. Markers require a serialized encoding input; `entryMarker_encoding_iff` preserves the marker condition for actual encoding calls. |
| [EncodingMarkerAllocation](../SphincsSecurity/Proof/EncodingMarkerAllocation.lean) | On the original contact law, $\mathbb E_R[\#\mathrm{marked}]\le1722\mathbb E_R A_{\rm enc}/N$. Repeated replies introduce no new markers, and the encoding count is exactly the recorded shared-budget charge. |
| [OtsMarkerContactBound](../SphincsSecurity/Proof/OtsMarkerContactBound.lean) | The sum of marker-checkpoint new-contact probabilities is at most $3444x\mathbb E_R A_{\rm enc}/[(1-x)N]$ for $q<N$, using the original shared encoding charge and the generalized fixed-endpoint restart proof. |
| [OtsContactMarkerBound](../SphincsSecurity/Proof/OtsContactMarkerBound.lean) | On the original full trace, the probability of a contact before that chain's first marker is at most $82x\mathbb E_R Q_{\rm prefix}/[(1-x)N]$ for $q<N$. History-dependent encoding charges are bounded by the original budget times the final contact count, whose expectation uses the shared prefix allocation. |
| [OtsMarkerContactProbability](../SphincsSecurity/Proof/OtsMarkerContactProbability.lean) | The probability that some chain is both marked and contacted on the original trace is at most $[82x\mathbb E_R Q_{\rm prefix}+3444x\mathbb E_R A_{\rm enc}]/[(1-x)N]$. The proved chronological partition pays the contact-first event once, then sums marker-first checkpoint events under an exact frontier-and-trace coupling. |
| [OtsTwoEdgeProbability](../SphincsSecurity/Proof/OtsTwoEdgeProbability.lean) | On the original full trace, $\Pr_R[\exists c:\mathrm{twoEdge}(c)]\le(3/2+4x+2x^2)\mathbb E_R Q_{\rm prefix}/[(1-x)N]$. The row correspondence preserves every observed prefix row exactly, and the joint bound retains the remaining original cost reservation. |
| [ReferenceLayerWitness](../SphincsSecurity/Proof/ReferenceLayerWitness.lean) | A layer recovering the canonical root either fixes its reference message, counter, chain values and siblings or supplies a queried structural, equal-encoding or OTS witness. Requires its actual query/reply pairs in the trace. |
| [VerifierContactWitness](../SphincsSecurity/Proof/VerifierContactWitness.lean) | The extracted OTS paths give the existing two-edge, distinct-contact and marker/contact events on a contact result with the same frontier and full trace. |
| [VerifierWitnessClassification](../SphincsSecurity/Proof/VerifierWitnessClassification.lean) | Complete verification yields an admissible digest and either `FullyHonestOpening`, selected reference openings and all required true FTS queries, or a queried layer/FTS exception. Requires canonical parameters and a trace containing the verifier rows. |
| [ReferenceVerifierSupport](../SphincsSecurity/Proof/ReferenceVerifierSupport.lean) | A successful supported original contact observer supplies a supported adversary output with a valid, strongly fresh signing log and the exact verifier rows in its full trace. |
| [EncodingMatchBound](../SphincsSecurity/Proof/EncodingMatchBound.lean) and [ReferenceEncodingContext](../SphincsSecurity/Proof/ReferenceEncodingContext.lean) | The original reference experiment satisfies $\Pr_R[\mathrm{equalEncoding}]\le\mathbb E_R A_{\rm enc}/N$. The exact conditional-source identity retains canonical messages for reference-input exclusion; cached rows cannot introduce a first match, and the charge is the existing recorded encoding allocation. |
| [ReferenceGraphProgram](../SphincsSecurity/Proof/ReferenceGraphProgram.lean), [ReferenceGraphContext](../SphincsSecurity/Proof/ReferenceGraphContext.lean) and [StructuralOracleObservation](../SphincsSecurity/Proof/StructuralOracleObservation.lean) | Exact original-reference source with the sampled key, graph and reference family retained. The signer and its costs depend only on the graph, selections and message rows; noncanonical structural rows have an exact lazy uniform-table representation. |
| [StructuralMatchBound](../SphincsSecurity/Proof/StructuralMatchBound.lean) | The original reference experiment satisfies $\Pr_R[\mathrm{structural}]\le\mathbb E_R A_{\rm other}/N$. The complete verifier classification retains proper payloads in the finite graph-input domain. The lazy cache invariant constrains only noncanonical cells, and the adaptive first-match bound transports exactly to the existing shared other-query allocation. |
| [ReferencePrimitiveBound](../SphincsSecurity/Proof/ReferencePrimitiveBound.lean) | Exact graph-context projections put the five primitive events on one original law. Their union is bounded with the existing expected prefix, encoding and other-query costs. For $q\le3\cdot2^{114}$, exact Lean arithmetic and the shared budget give $\Pr_R[E_{\rm primitive}]+(7/4)\mathbb E_R A_{\rm msg}/N\le(7/4)x$. |
| [OriginalCertificateBound](../SphincsSecurity/Proof/OriginalCertificateBound.lean) and [CertificateOriginalMessageCost](../SphincsSecurity/Proof/CertificateOriginalMessageCost.lean) | The original key/log/cache source projects exactly to the SUF verdict. A passive bank monitor bounds its full-certificate event by $\mathbb E D_{\rm original}/N+\delta x+\Pr[E_{\rm certificate}]$, with only the original hash bound and $q\le2^{127}$. The paid counter is bounded by actual original message work through an exact cache projection. The reference message-allocation, cache-history and proposal-prefix bounds are proved below. |
| [OriginalMessageAllocation](../SphincsSecurity/Proof/OriginalMessageAllocation.lean) | Original certificate message work is at most the shared reference message count, through the original boundary law and its exact reference projection. |
| [OriginalCacheExceptionBound](../SphincsSecurity/Proof/OriginalCacheExceptionBound.lean) | The actual certificate monitor's cache-history probability is at most $\mathbb E D_{\rm original}\,r_{\rm cache}\le q/2^{169}$. Freezing the weight at one after a hit and charging actual message work handles adaptive macro boundaries, including after the certificate monitor stops. The shared reservation absorbs this charge: $\Pr_R[E_{\rm primitive}]+\Pr_{\rm original}[C_{\rm full}]\le(7/4+\delta)x+\Pr[E_{\rm prefix}]$ for $q\le3\cdot2^{114}$. The proposal-prefix bound is proved below. The SUF connection is proved below; the remaining FTS estimates are still required. |
| [OriginalProposalPrefixBound](../SphincsSecurity/Proof/OriginalProposalPrefixBound.lean) | The original monitor preserves the expected exponential prefix weight through active signing, ordinary queries and inactive steps. The supported signing-log cap makes the weight dominate a prefix exception, giving probability at most $2^{-700}$. Thus $\Pr_R[E_{\rm primitive}]+\Pr_{\rm original}[C_{\rm full}]\le(7/4+\delta)x+2^{-700}$ for $q\le3\cdot2^{114}$. The SUF connection is proved below; the remaining FTS estimates are still required. |
| [ReferenceForgerySource](../SphincsSecurity/Proof/ReferenceForgerySource.lean) | The actual retained forgery has the original SUF verdict and primitive-event law. Its successful valid signing history yields a full certificate, near certificate with an undisclosed true guess, two distinct undisclosed true guesses, or a primitive event. |
| [ReferenceCertificateCoverage](../SphincsSecurity/Proof/ReferenceCertificateCoverage.lean) | The joint retained key, forgery, signing log, verification result and message-query record have the original execution's law. The transcript certificate's required rows embed in the original cache, preserving own-input exclusion. For $q\le3\cdot2^{114}$, original SUF success is at most $(7/4+\delta)x+2^{-700}+\Pr_R[E_{\rm remainingFTS}]$. The remaining event refers to the actual retained forgery and valid signing log. |

## Remaining work

1. Establish fresh-message and signing kernels in each forced-FTS law. Use the record-and-length projection, attach that law's own proposal words, and bound the retained near-certificate/guess and two-distinct-guesses events. The original SUF decomposition, certificate transfer and shared primitive/certificate reservation are proved.
2. Insert those two estimates into `forgeAdvantage_le_remainingFts_small_budget`, close the small-range arithmetic, and combine it with `security127_of_large_budget`. Audit the resulting public theorem without additional cryptographic premises.

The [small-range gate audit](small-range-gate-audit.md) specifies why secret-dependent canonical high halves cannot enter the OTS history, and why the forced-FTS comparison needs no joint posterior for rejected proposal values. The full-cache exceptional flag is also excluded from that projected stopping rule: its probability is paid on the original law using the spent-call invariant.

## First contact with prepared suffixes

[PartialChainSuffix](../SphincsSecurity/Proof/PartialChainSuffix.lean) bounds the endpoint density $W(T,Y)$ by one plus the number of nonempty, fully queried suffix paths ending at $Y$. [PartialChainCompletion](../SphincsSecurity/Proof/PartialChainCompletion.lean) counts every completed suffix by its starting queried row. Thus completed suffixes plus pending queried rows equal the number of distinct queried rows.

Let $P(T)$ be the pending count. Before any contact with $Y$, recording one consistent row gives $W(T',Y)+P(T')\le P(T)+2$. This pays for a final query that completes several previously prepared paths. [PartialChainLastRow](../SphincsSecurity/Proof/PartialChainLastRow.lean) proves that the pending count after a last-level query is independent of its answer, and that the density is a martingale under a lazy row observation.

[PartialChainContactPotential](../SphincsSecurity/Proof/PartialChainContactPotential.lean) uses potential $P(T)/N$ before contact and $W(T,Y)$ afterward. Its expected increase is at most $2/N$ per prefix query. [AdaptiveChainPotential](../SphincsSecurity/Proof/AdaptiveChainPotential.lean) sums such drift against the actual expected query counter, allowing adaptive queries and auxiliary randomness. The exact likelihood identity then gives `realRun_contact_le`: $\Pr_R[\mathrm{contact}]\le2\mathbb E_I Q/N$.

[AdaptiveChainCapObservation](../SphincsSecurity/Proof/AdaptiveChainCapObservation.lean) now identifies the uncapped and capped real contact events while preserving their observed prefix rows. [QueryCapBalance](../SphincsSecurity/Proof/QueryCapBalance.lean) accounts for every query through the cap, and [AdaptiveChainCapContact](../SphincsSecurity/Proof/AdaptiveChainCapContact.lean) identifies the completed ideal run's explicit counter with its spent-budget field. [OtsPrefixContactProbability](../SphincsSecurity/Proof/OtsPrefixContactProbability.lean) averages this bound over the actual conditional source samples and retains the encoding, other-hash and native message reservation when summing over chains.

[AdaptiveChainRestart](../SphincsSecurity/Proof/AdaptiveChainRestart.lean) also allows a nonempty initial partial table with no contact with the fixed endpoint. If $a$ is its distinct queried-row count and $Q^+$ counts future queries, the lemma proves $\mathbb E_I[W_{\rm final}\mathbf1_{\rm contact}]\le\mathbb E_I[a+2Q^+]/N$. Its companion lemma transfers this charge using the likelihood lower bound on the final observed table. The checkpoint law and its weighted restart transfer are now proved below. The first-contact stop, common trace source transport, checkpoint event comparisons and expected cost allocation are instantiated below. The distinct-chain and two-edge probability estimates are established. The original marker-count estimate is proved in [EncodingMarkerAllocation](../SphincsSecurity/Proof/EncodingMarkerAllocation.lean), with $\mathbb E_R[\#\mathrm{marked}]\le1722\mathbb E_R A_{\rm enc}/N$ and exact transport of the shared encoding charge. Both chronological marker/contact estimates are proved. The chronological marker/contact partition and combined event bound are proved below. The original-game witness classification linking these events to forgery and the forced-FTS estimates remain open.

## Adaptive checkpoints and visible external queries

[AdaptiveChainCheckpoint](../SphincsSecurity/Proof/AdaptiveChainCheckpoint.lean) retains the endpoint, the first program's result and observed rows, and the continuation's result, count and rows. It proves the exact posterior formula at the checkpoint and the final likelihood identity for any nonnegative statistic of this joint result. Supported real checkpoints and continuations also lie in their corresponding lazy supports. This derives the observation law from the existing query interpreter without assuming independence of the stopping decision and earlier answers.

For a marked checkpoint at which the selected chain has not been contacted, [AdaptiveChainCheckpointContact](../SphincsSecurity/Proof/AdaptiveChainCheckpointContact.lean) transports the allocated charge $a+2Q^+$ to the joint real law. [QueryPause](../SphincsSecurity/Proof/QueryPause.lean) builds an actual pause from a predicate on memory updated after each query. Resuming gives the original program, and its counted pause proves that the prefix already spent plus the remaining bound fits the original budget. [AdaptiveChainPause](../SphincsSecurity/Proof/AdaptiveChainPause.lean) discharges the restart's row and cost hypotheses from this one budget and proves $(1-x)N\Pr_R[\mathrm{marked},\neg\mathrm{contactBefore},\mathrm{contactAfter}]\le2q\Pr_R[\mathrm{marked}]$. The allocated version remains necessary when summing over possible target chains; applying the scalar version separately would lose the shared charge.

The earlier `OtsPrefix.seedGame` replaces every hash call outside the selected prefix with a pure fixed answer. Its remaining query stream therefore cannot directly drive a monitor for contacts with other chains or encoding markers. [OtsPrefixVisible](../SphincsSecurity/Proof/OtsPrefixVisible.lean) retains those calls as explicit auxiliary queries with the same fixed answers. Erasure recovers the old seed program and preserves the real endpoint, result, hash-cost trace and selected-prefix rows jointly. Erasure also commutes with the selected counter and analytical cap, preserving the capped real and ideal laws used by the existing budget comparison. The program is still unchanged by replacement of the selected private starting-secret field.

[OtsPrefixVisibleTrace](../SphincsSecurity/Proof/OtsPrefixVisibleTrace.lean) reconstructs each selected-prefix query's original bytes and full output from its low answer and independent high half, and logs outside hash queries directly. Its trace equals the external hash input/output trace of the common `CausalFrontierProgram`. [QueryPauseTranslation](../SphincsSecurity/Proof/QueryPauseTranslation.lean) then proves that a stop on this trace commutes with the interpreter: both sides retain the same trace and corresponding continuation. The first-contact stop is now instantiated in [OtsContactPause](../SphincsSecurity/Proof/OtsContactPause.lean). Its trace holds at most one contacted chain because a query selects at most one address. [OtsContactSplit](../SphincsSecurity/Proof/OtsContactSplit.lean) proves that a completed trace with two contacted chains has a marked checkpoint and a new contact afterward. It recovers the original full trace and bounds its length by the original hash-cost trace. [OtsContactAllocation](../SphincsSecurity/Proof/OtsContactAllocation.lean) sums the restart charge over uncontacted chains using this common split. [OtsContactCheckpoint](../SphincsSecurity/Proof/OtsContactCheckpoint.lean) identifies checkpoint trace contacts with lazy partial-table contacts, including repeated queries, and bounds observed rows by the selected trace count. [ReferenceContactGame](../SphincsSecurity/Proof/ReferenceContactGame.lean) retains the frontier and this split alongside the original game result. [OtsContactSourceAllocation](../SphincsSecurity/Proof/OtsContactSourceAllocation.lean) proves that every conditional chain source has exactly this common law and bounds the expected sum of marked restart charges by $2q$ times the first-contact probability. [OtsContactCheckpointLaw](../SphincsSecurity/Proof/OtsContactCheckpointLaw.lean) projects the joint real checkpoint and traced continuation to that conditional law, preserving the final partial-table rows. [OtsTraceCheckpointObservation](../SphincsSecurity/Proof/OtsTraceCheckpointObservation.lean) proves those final-contact and future-count comparisons, and [OtsContactProbability](../SphincsSecurity/Proof/OtsContactProbability.lean) averages the allocated restart estimate onto the common original law. The original SUF classification and forced-FTS estimates remain open.

[QueryCapState](../SphincsSecurity/Proof/QueryCapState.lean) removes a remaining budget obstacle. Erasing the state of the capped execution gives exactly the result obtained by applying the cap's finish function to the complete counted execution. Thus the already proved absence of a capped ideal halt excludes over-budget uncapped ideal counts. [AdaptiveChainActualBudget](../SphincsSecurity/Proof/AdaptiveChainActualBudget.lean) applies this fact at every endpoint and at a pause followed by its continuation. The allocated restart inequality in `pausedRun_newContact_charge_of_real` now needs the structural query-cost charge, the real whole-run cost bound and $q<N$, with no separate syntactic query bound.

[OtsContactFirstLaw](../SphincsSecurity/Proof/OtsContactFirstLaw.lean) identifies a full-trace contact with the corresponding final observed-table contact, preserving those rows through observer erasure. [OtsContactFirstProbability](../SphincsSecurity/Proof/OtsContactFirstProbability.lean) averages this exact event identity and proves $(1-x)\Pr_R[\mathrm{firstContact}]\le2\mathbb E_R Q_{\rm prefix}/N$. [OtsDistinctContactProbability](../SphincsSecurity/Proof/OtsDistinctContactProbability.lean) classifies two contacted chains into a first-contact checkpoint and a new chain afterward. Its union bound and the shared restart allocation give $(1-x)N\Pr_R[E_2]\le2q\Pr_R[\mathrm{firstContact}]$.

[OtsDistinctContactBound](../SphincsSecurity/Proof/OtsDistinctContactBound.lean) combines these inequalities into $\Pr_R[E_2]\le4x\mathbb E_R Q_{\rm prefix}/[(1-x)^2N]$ for $q<N$. The joint version retains the original expected encoding, other-hash and message charges: $(1-x)^2N\Pr_R[E_2]+4x\mathbb E_R[A_{\rm enc}+A_{\rm other}+A_{\rm msg}]\le4xq$. This pays the distinct-chain event with the actual shared prefix allocation, using only the original hash bound and the budget range.

[OtsContactMarkerBound](../SphincsSecurity/Proof/OtsContactMarkerBound.lean) proves the reverse chronological estimate on the original full trace: $\Pr_R[\mathrm{contactBeforeFirstMarker}]\le82x\mathbb E_R Q_{\rm prefix}/[(1-x)N]$. The event in [OtsContactMarkerTrace](../SphincsSecurity/Proof/OtsContactMarkerTrace.lean) explicitly splits the trace at a new marker whose chain was already contacted. Its counter increases only at such markers. The conditional row bound charges $41/N$ per already contacted chain, and [EncodingContactMarkerAccumulation](../SphincsSecurity/Proof/EncodingContactMarkerAccumulation.lean) accumulates that history-dependent charge through arbitrary adaptive programs without losing probability mass. Each run's accumulated charge is at most its trace length times its final contact count. The exact source transport retains the frontier together with the full trace; the original whole-experiment budget then bounds the length by $q$. The expected final contact count is the sum of the concrete per-chain contact probabilities and is bounded by the existing shared expected prefix allocation. This proves the factor 82 without allocating $q$ independently to each chain. The chronological partition joins the two estimates below; connecting the combined event to the original accepting-forgery classification remains required.

[OtsMarkerContactPartition](../SphincsSecurity/Proof/OtsMarkerContactPartition.lean) completes the chronological split. At the first-marker pause, a selected chain that is already contacted gives a contact-before-first-marker witness: the new marker cannot itself contact any prefix. The pause retains that witness under continuation. If the complete trace contains the marker, the pause has seen it, including when the computation ends immediately after that query. Consequently, outside the global contact-before-marker event, every chain that is marked and contacted is covered by its marker-first checkpoint event. [OtsMarkerContactProbability](../SphincsSecurity/Proof/OtsMarkerContactProbability.lean) transports each residual event through the joint frontier-and-trace law and proves $\Pr_R[\exists c:\mathrm{marked}(c)\land\mathrm{contacted}(c)]\le[82x\mathbb E_R Q_{\rm prefix}+3444x\mathbb E_R A_{\rm enc}]/[(1-x)N]$. The contact-first event is paid once before the finite union over the residual chains. Only the original hash-bound premise and $q<N$ are needed.

## Two-edge transition bounds and their accumulation

The one-chain counter $K$ in [PartialChainContactCount](../SphincsSecurity/Proof/PartialChainContactCount.lean) counts distinct queried last-level rows returning the endpoint. It does not count contacted chains. Fresh last rows increment it with probability $1/N$, and repeated rows leave it unchanged. [AdaptiveChainBudgetPotential](../SphincsSecurity/Proof/AdaptiveChainBudgetPotential.lean) supports a potential indexed by remaining query budget. Applying it to $K(K-1)+2bK/N$ proves the factorial moment under adaptive allocation and stopping. [AdaptiveChainContactMoments](../SphincsSecurity/Proof/AdaptiveChainContactMoments.lean) combines this with the first moment to obtain the exact correction required by the paper calculation.

[PartialChainTwoEdge](../SphincsSecurity/Proof/PartialChainTwoEdge.lean) classifies the first completion and proves that, before it, every nonempty fully queried suffix to the endpoint is a last-edge hit. Thus $W\le1+K$. [PartialChainRowCharge](../SphincsSecurity/Proof/PartialChainRowCharge.lean) proves that a row update adds at most the incoming prefix density, then bounds that density by one plus its fully queried suffix paths. This gives the weighted first-completion bounds in both chronological orders.

[PartialChainLongCompletion](../SphincsSecurity/Proof/PartialChainLongCompletion.lean) and [PartialChainPreparation](../SphincsSecurity/Proof/PartialChainPreparation.lean) formalize the accumulated preparation. Let $D$ count completed suffix paths of length at least two to any endpoint, let $P$ count queried last-row inputs reached by queried penultimate rows, and let $R$ count completed suffixes of length at least two after removing the last level. A fresh last query adds its earlier-path count to $D$, and adds one to $P$ when productive. A fresh penultimate query adds its earlier-path count to $R$. These counts are monotone under consistent observations. With $s$ selected queries spent, [PartialChainTwoEdgeCharge](../SphincsSecurity/Proof/PartialChainTwoEdgeCharge.lean) uses $A=D+2P+sK(K+2)+KR$ and proves $A\le3Q/2+q[K(K-1)+4K]$ whenever $Q\le s\le q$.

[PartialChainTwoEdgeCompensation](../SphincsSecurity/Proof/PartialChainTwoEdgeCompensation.lean) proves $A/N+\mathbb E[B']\le B+\mathbb E[A']/N$, where $B=W\mathbf1_{\rm twoEdge}$. Before completion, the charge increases pay both chronological orders. After completion, the density martingale and monotone charge give the inequality. [AdaptiveChainCompensation](../SphincsSecurity/Proof/AdaptiveChainCompensation.lean) telescopes it through arbitrary adaptive query programs, checking that the canceled one-step expected charge is finite. Thus [AdaptiveChainTwoEdge](../SphincsSecurity/Proof/AdaptiveChainTwoEdge.lean) combines the terminal charge with the established moments to prove $\Pr_R[\mathrm{twoEdge}]\le(3/2+4x+2x^2)\mathbb E_I Q/N$ from an empty observed table.

[AdaptiveChainCapTwoEdge](../SphincsSecurity/Proof/AdaptiveChainCapTwoEdge.lean) transfers the uncapped real event to the analytical cap and expresses the estimate using its ideal spent cost. Prefixes shorter than two edges have an empty event. [OtsPrefixTwoEdgeProbability](../SphincsSecurity/Proof/OtsPrefixTwoEdgeProbability.lean) averages this kernel over the concrete conditional source. With $r=(3/2+4x+2x^2)/N$, it proves $(1-x)\sum_c\Pr_R[E_c]+r\mathbb E_R[A_{\rm enc}+A_{\rm other}+A_{\rm msg}]\le rq$ using only the original hash bound and $q<N$. The full-trace correspondence below transports this estimate to the original event; classifying every relevant original forgery into this and the other paid events remains required.

[OtsTraceRows](../SphincsSecurity/Proof/OtsTraceRows.lean) gives the exact row relation needed for completed path witnesses: a selected-prefix row is present in the final observed table if and only if a full hash entry in the retained trace parses to that row and has the same low reply. The relation is preserved by each visible query, including cached replies, and through arbitrary adaptive programs. [OtsTraceRowSource](../SphincsSecurity/Proof/OtsTraceRowSource.lean) transports it to the original contact observer, preserving final rows when combining the trace fragments. [OtsTwoEdgeTrace](../SphincsSecurity/Proof/OtsTwoEdgeTrace.lean) describes a two-edge witness using the final two prefix levels and proves its exact equivalence with `TwoEdgeEvent`, including depths zero and one. [OtsTwoEdgeSource](../SphincsSecurity/Proof/OtsTwoEdgeSource.lean) and [OtsTwoEdgeProbability](../SphincsSecurity/Proof/OtsTwoEdgeProbability.lean) identify its per-chain law with `prefixTwoEdgeGame` and then bound the original union by $(3/2+4x+2x^2)\mathbb E_R Q_{\rm prefix}/[(1-x)N]$. The joint version retains the original expected remaining costs. These are bounds for actual original-trace events. The layer extraction in [ReferenceLayerWitness](../SphincsSecurity/Proof/ReferenceLayerWitness.lean) now supplies the corresponding OTS witnesses from a canonical recovered tree root and actual verifier rows. The complete hypertree and FTS extraction is now proved in [VerifierWitnessClassification](../SphincsSecurity/Proof/VerifierWitnessClassification.lean), and [ReferenceVerifierSupport](../SphincsSecurity/Proof/ReferenceVerifierSupport.lean) supplies actual verifier rows from the original contact source. [ReferenceVerifierInstantiation](../SphincsSecurity/Proof/ReferenceVerifierInstantiation.lean) now instantiates the original sampled reference family, retains the signing-history invariant and proves that every successful signing entry has a different message-hash payload from an honest strong forgery. [ReferenceSigningReplay](../SphincsSecurity/Proof/ReferenceSigningReplay.lean) establishes replay without a disclosure premise, and [FrontierSigningOrigin](../SphincsSecurity/Proof/FrontierSigningOrigin.lean) recovers the actual signer and its recorded digest row for each successful entry. [ReferenceForgeryCoverage](../SphincsSecurity/Proof/ReferenceForgeryCoverage.lean) now gives the full-certificate, near-certificate with one undisclosed true query, and two-distinct-undisclosed-query alternatives, alongside the existing OTS contact events and encoding or structural matches. Certificates use actual recorded message rows and eligible successful signing entries. Structural matches retain valid graph positions and stay above OTS reference frontiers. The equal-encoding estimate is now proved in [EncodingMatchBound](../SphincsSecurity/Proof/EncodingMatchBound.lean), with the existing shared encoding charge and canonical messages retained in its source law. The structural estimate is also proved in [StructuralMatchBound](../SphincsSecurity/Proof/StructuralMatchBound.lean), using the existing shared other-query charge. The primitive union and its shared small-budget reservation are now proved in [ReferencePrimitiveBound](../SphincsSecurity/Proof/ReferencePrimitiveBound.lean). The forced-FTS estimates and final original-SUF combination remain open.

## Shortcuts that do not close the current estimates

Two tempting numerical shortcuts do not remove the small-budget obligation with the current estimates. First, crediting the established honest-work debits is insufficient. At $q=2^{112}$, $x=2^{-16}$ and $\delta=11/2^{16}$, the unshifted excess $\delta x-x^2$ is $10/2^{32}$ before exceptions. Even replacing $x$ in the primitive term by $(q-K)/2^{128}$ with $K=1212415+2^{24}\cdot28504$ leaves excess greater than $9/2^{32}$. This calculation grants the minimum debit for every allowed signing request; it is a generous arithmetic check, not a valid uniform debit for executions making fewer requests. Reclaiming key generation alone also fails this check. A sharper bound tied to the actual adaptive allocation would need a new argument.

Second, the existing terminal-price moments cannot justify replacing a correlated weighted price by its mean. For an abstract nonnegative price $P$ equal to $2$ with probability $10^{-4}$ and zero otherwise, $\mathbb E P\le1/5$ and $\operatorname{Var}(P)\le13/25000$. Taking a bounded charge $M=q\mathbf1_{P=2}$ nevertheless gives $\mathbb E[MP]=2\mathbb E[M]$. This is a counterexample to an inference from the moment bounds alone, not an attack on the scheme or a claim about its exact price distribution. The terminal-price excess term accounts for precisely this possible dependence; removing it requires more than an average-price estimate.

Sharpening the exact terminal-price tail alone cannot eliminate the small-budget argument either. In the existing uniform proposal word, put $m=2^{26}$, $L=25313293$, and $P=2^{-48}\sum_i R_i^{14}$, where $R_i$ is the number of occurrences of index $i$. Let $A_i$ mean $R_i=12$. The binomial and multinomial formulas, followed by Bernoulli's inequality and the first two inclusion-exclusion terms, give

\[
\Pr\Bigl[\bigcup_i A_i\Bigr]\ge m\frac{(L-11)^{12}}{12!m^{12}}\left(1-\frac{L-12}{m}\right)-\binom m2\frac{L^{24}}{(12!)^2m^{24}}>2^{-21}.
\]

On this event $P\ge12^{14}/2^{48}>4$, so $\mathbb E[(P-2)_+]>2^{-20}$. The two strict numerical inequalities were checked with exact rational arithmetic; this is a paper calculation, not a new Lean theorem. Even allowing a baseline payment of $2$ instead of $1$ therefore leaves a positive excess in this terminal-price estimate. At $q=2^{100}$, its coefficient exceeds $x=2^{-28}$, so a bound of the form $2x-x^2+\eta x$ with this excess coefficient $\eta$ does not close. This concerns the analytical proposal envelope, not an attack or a lower bound on forgery probability. A sharper joint allocation argument could still improve the result; computing higher moments of this same envelope alone cannot remove the separate small-budget obligation.

## Budget transfer using a prefix cap

The cap stops a prefix program before its $(q+1)$st query to the selected prefix. `QueryCap.run_queryBound` proves the syntactic bound required by `realRun_empty_cost_lower`. `QueryCap.run_eq_counted` identifies its distribution exactly: complete counted runs that exceed the budget map to the ordinary cap result, retaining all probability mass. `seedGame_counted_le` bounds the selected-prefix count by the original hash-cost trace, including verifier work. Consequently `realRun_cap_erased` recovers the uncapped real output distribution after removing the cap's remaining-budget field. `realRun_cap_recover_count` also recovers the joint distribution of the original result and its prefix-query count by subtracting the remaining budget from the initial budget.

For $q<N$, the capped likelihood lower bound has positive coefficient $1-q/N$. Its pointwise form puts the entire ideal support inside real support, including the cap result and observed prefix table. `idealRun_cap_valid` therefore excludes cap termination and bounds every completed ideal result's original cost by $q$. `prefixCappedSeedGame_contract` instantiates this argument for the concrete source using only the original hash bound, the budget range and membership of the fixed samples in their source supports.

This establishes the bounded conditional interface without an operational cap inside omitted private signing work. `referenceRecordedGame_allocation_le` and `referenceRecordedGame_expected_allocation_le` supply one original-game budget for the sum over all OTS chains. `game_counted_source` identifies each fixed-oracle interpreter count with its charge in the common program. `prefixCountedObservedGame_original` completes the counter transport through conditional seed sampling. `prefixIdealCostGame_shared_budget` then gives $(1-q/N)\sum_c\mathbb E Q_c^{\rm ideal}\le q$ by averaging the capped likelihood comparison and applying the common original budget. `prefixIdealCostGame_joint_budget` retains the original expected encoding, other non-message and native message charges beside the scaled ideal OTS cost. Its external charges count all calls, providing an upper bound for the later fresh-query subsets. The concrete first-contact probability bound now uses this allocation with observed-row cap erasure. The fixed-endpoint restart charge includes earlier preparation. The adaptive checkpoint law, a budget-preserving pause, and the concrete visible hash trace are now established. The distinct-chain stopped allocation and probability bound are now connected; the remaining completed-witness classification and forced-FTS bounds are still required. The original scheme and budget definition remain unchanged.

The structural lazy sampler keeps canonical queries in its fixed auxiliary oracle. They do not disclose the corresponding unused residual table cells. [StructuralTraceCache](../SphincsSecurity/Proof/StructuralTraceCache.lean) therefore constrains only noncanonical structural cells and proves that a cached reply cannot introduce a first match. Queries outside the finite sampled domain use the fixed auxiliary reply. [GraphPayloadInputs](../SphincsSecurity/Proof/GraphPayloadInputs.lean) proves that every extracted structural witness uses a payload in the finite domain, and the complete verifier classification retains that membership. [StructuralMatchAccumulation](../SphincsSecurity/Proof/StructuralMatchAccumulation.lean) uses these facts to bound the adaptive match probability; [StructuralMatchBound](../SphincsSecurity/Proof/StructuralMatchBound.lean) identifies its expected charge with the original recorded other-query allocation.
