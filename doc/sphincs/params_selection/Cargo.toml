[package]
name = "sphincs_params"
version = "0.1.0"
edition = "2024"

# Its own workspace: a parameter-exploration tool for doc/, not part of the
# proving stack, and nothing in crates/ depends on it.
[workspace]

[dependencies]

[lints.clippy]
too_many_arguments = "allow"

[profile.release]
lto = "thin"
codegen-units = 1
